Set-Content app.py "from flask import Flask, request, jsonify`nfrom flask_cors import CORS`nfrom transformers import AutoTokenizer, AutoModelForSeq2SeqLM`nfrom peft import PeftModel`nimport torch`n`napp = Flask(__name__)`nCORS(app, origins='*')`n`nMODEL_PATH = r'.\models\English-Igbo_NLLB_FT_model'`nBASE_MODEL = 'facebook/nllb-200-distilled-600M'`n`nprint('Loading model...')`ntokenizer = AutoTokenizer.from_pretrained(MODEL_PATH)`nbase_model = AutoModelForSeq2SeqLM.from_pretrained(BASE_MODEL)`nmodel = PeftModel.from_pretrained(base_model, MODEL_PATH)`nmodel.eval()`ntgt_id = tokenizer.convert_tokens_to_ids('ibo_Latn')`nprint('Model ready! Lang ID:', tgt_id)`n`n@app.route('/translate', methods=['POST', 'OPTIONS'])`ndef translate():`n    if request.method == 'OPTIONS':`n        r = jsonify({})`n        r.headers['Access-Control-Allow-Origin'] = '*'`n        r.headers['Access-Control-Allow-Headers'] = 'Content-Type'`n        r.headers['Access-Control-Allow-Methods'] = 'POST'`n        return r`n    try:`n        text = request.json.get('text', '')`n        inputs = tokenizer(text, return_tensors='pt')`n        with torch.no_grad():`n            outputs = model.generate(**inputs, forced_bos_token_id=tgt_id, max_length=200)`n        translation = tokenizer.decode(outputs[0], skip_special_tokens=True)`n        resp = jsonify({'translation': translation})`n        resp.headers['Access-Control-Allow-Origin'] = '*'`n        return resp`n    except Exception as e:`n        resp = jsonify({'error': str(e)})`n        resp.headers['Access-Control-Allow-Origin'] = '*'`n        return resp, 500`n`nif __name__ == '__main__':`n    app.run(port=5002, debug=False)"





Then verify with type app.py and run python app.py!